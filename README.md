To run locally you will need to create a config.json
```
{
    "prefix": "!",
    "token": "<<Your Bot Token Here>"
}
```

Then you can use nodemon to launch the bot.
```
nodemon --inspect index.js
```